# Phase 1: Environment & Project Workspace Setup

## Goals
Establish clean, isolated project directories, configure python dependencies, and build helper modules for loading datasets and verifying paths.

## Checklist
- [ ] Create `Candidate_Submission/Task1_Solution` & `Candidate_Submission/Task2_Solution`.
- [ ] Create consolidated `requirements.txt` with dependencies (`torch`, `torchvision`, `opencv-python-headless`, `pillow`, `numpy`, `reportlab`, `matplotlib`).
- [ ] Create `dataset_utils.py` to parse CSVs (`labels_train.csv`, `labels_dev.csv`, `meta_train.csv`, `meta_dev.csv`).
- [ ] Test data loading scripts on `images/train` and `images/dev`.

## Verification
- Run python check to verify imports and dataset accessibility without error.
